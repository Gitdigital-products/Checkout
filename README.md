# Checkout
 Internal fork of GitHub’s actions/checkout, maintained by Gitdigital-products.  This repository exists to satisfy organization security policies requiring all GitHub Actions to be:  Owned by the Gitdigital-products organization  Pinned to immutable commit SHAs  Reviewed and controlled internally   The action checks out a repository so work
